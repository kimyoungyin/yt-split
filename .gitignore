.claude/
.venv/
__pycache__/
*.pyc
output/
downloads/

# PyInstaller
build/
dist/

# Tauri sidecar binaries (built locally; produced by pyinstaller/build.sh)
src-tauri/binaries/
src-tauri/target/

# Frontend
ui/node_modules/
ui/dist/

# Root-only node_modules / lockfile (root package.json has no deps; pnpm
# creates these as a side effect of `pnpm install` in the repo root).
/node_modules/
/pnpm-lock.yaml
